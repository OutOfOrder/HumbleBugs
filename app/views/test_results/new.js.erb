$("<div id=\"test_results_dialog\"><%= escape_javascript render 'form' %></div>").dialog({
    modal:true,
    title:'New test result',
    width: '40em',
    close:function () {
        $(this).remove();
    }
});