<% if @reload %>
$('#comments_dialog').closest(':ui-dialog').dialog('close');
document.location.reload();
<% else %>
$('#comments_dialog').closest(':ui-dialog').dialog('close');
Controls.Setup($('#comments').append("<%= escape_javascript render(@comment) %>").last('.comment'));
<% end %>