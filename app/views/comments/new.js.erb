$("<div id=\"comments_dialog\"><%= escape_javascript new_form %></div>").dialog({
    modal:true,
    title:'New Comment',
    width: '40em',
    close:function () {
        $(this).remove();
    }
});