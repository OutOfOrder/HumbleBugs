<ul>
  <% issues.each do |issue| %>
    <li><%= link_to "View ##{issue.id}", [issue.game, issue], title: "For game #{issue.game.name}" %> created <%= timeago(issue.created_at) %></li>
  <% end %>
</ul>