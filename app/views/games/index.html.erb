<% @bundles.each do |bundle| %>
    <% unless bundle.games.empty? %>
        <fieldset class="game_listing">
          <legend><%= bundle.name %></legend>
          <%= render :partial => 'games', :locals => {:games => bundle.games} %>
        </fieldset>
    <% end %>
<% end %>
<% unless @games.empty? %>
    <fieldset class="game_listing">
      <legend>Unassigned</legend>
      <%= render :partial => 'games', :locals => {:games => @games} %>
    </fieldset>
<% end %>
